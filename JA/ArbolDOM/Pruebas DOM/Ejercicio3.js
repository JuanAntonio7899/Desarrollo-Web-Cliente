function suma() {
    let sum =  (document.getElementsByTagName("div")[0]);
    sum.textContent = parseInt(sum.textContent)+1;

    
    

    
}

function resta() {
    let resta =  (document.getElementsByTagName("div")[0]);
    resta.textContent = parseInt(resta.textContent)-1;

    console.log(resta);
    

    
}